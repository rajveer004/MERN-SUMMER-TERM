const product = {
    name : 'Socks',
    priceCents : 1090,
    inStock : true
}
console.log("name of product is : "+product.name +" and price is : "+product.priceCents);
console.log(`product name is : ${product.name} and price is ${product.priceCents}`);